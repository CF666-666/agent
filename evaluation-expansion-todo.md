# 评测规模扩大待做清单（简历数据保真）

> 状态：待执行 | 关联文档：[评测报告](evaluation-report.md) | 创建日期：2026-08-12
> 目的：让简历中所有评测数字真实、可复现、经得起面试现场验证。

## 一、背景

当前简历描述的评测数据与项目实际评测规模存在差距：

- 简历第 2 条写"固定 **100 条**图纸评测集"，实际图纸评测为 **25 条**（12 张图构造），全链路 100 条中 image 场景仅占 25 条。
- 简历第 4 条 RAGAS 指标未写条数，实际仅 **12 条**（官方文档已列 L3 局限）。
- 主评测集每层仅 25 条，单层统计置信区间约 ±20%，样本偏少。

为保障简历数据真实，制定以下扩大计划。

## 二、当前实际规模（基线）

| 维度 | 当前值 | 来源 |
|---|---|---|
| 全链路主评测集 | 100 条（fact/colloquial/image/relation 各 25） | `scripts/eval/datasets/industrial_eval_v2.jsonl` |
| 图纸评测集 | 25 条 | `scripts/eval/report/phase82h_D_image_*.json` |
| RAGAS 生成评测 | 12 条 | `scripts/eval/report/` RAGAS 部分 |
| 图纸/图像素材 | 12 张 | `bootstrap/data/images/` |

## 三、目标规模

| 维度 | 目标 | 简历表述 |
|---|---|---|
| 图纸评测集 | **100 条** | "固定 100 条图纸评测集 Hit@1=xx%、P95=xx" |
| 主评测集 | **250 条**（fact 50 / colloquial 50 / image 100 / relation 50） | "250 条分层评测 Hit@1/3/5=xx%、MRR=xx、P95=xx" |
| RAGAS | **60 条分组**（故障/规程/保养/安全 各 15） | "RAGAS 60 条分组评测" |

> 注意：主集指标将随样本扩大重跑，现有 79%/81%/81%、MRR=0.7994、P95=10.1s 数字会失效，需以新报告替换。

## 四、任务清单（按执行顺序）

### P0：图纸评测集扩到 100 条

- [ ] 收集/整理工业图纸与现场图像，目标 **40-50 张**（新增 30-40 张）
- [ ] 为新增图纸生成 Qwen-VL 语义描述（复用 `descriptions.jsonl` 结构）
- [ ] 扩展 `build_dataset_v2.py` 的 image 场景构造逻辑，生成 **100 条** image 评测样本（每张图约 2-3 条，模板轮换）
- [ ] 重跑图纸场景评测，记录 Hit@1 / P95
- **验收**：image 场景样本数=100，来源 ID 可校验，报告落盘 `scripts/eval/report/`

### P1：主评测集扩到 250 条（每层至少 50）

- [ ] fact 层补 25 条（从 FAQ 210 池子按类别分层抽取，避免与现有 25 条重复）
- [ ] colloquial 层补 25 条（复用口语模板 + 增加同义改写/省略/错字变体）
- [ ] relation 层补 25 条（从超边 633 池子抽取，注意与现有 25 条不重复、覆盖更多设备）
- [ ] image 层已由 P0 扩到 100 条，无需重复
- [ ] 重跑全链路评测，产出新 Hit@1/3/5、MRR、P95
- **验收**：主集样本数=250，各层样本分布正确，新报告落盘

### P2：RAGAS 扩到 60 条分组评测

- [ ] 按故障诊断/操作规程/维护保养/安全规范四类**各选 15 条**（共 60 条）
- [ ] 修改 `ragas_eval.py` 支持场景分组统计
- [ ] 并行执行 60 条（预计约 1200 次 LLM 调用，成本十几元，并行 30-40 分钟）
- [ ] 输出分组报告（各场景 faithfulness / context_precision / context_recall）
- **验收**：60 条全部完成、分组指标落盘、无超时丢弃

### P3：简历数据表述联动修正

- [ ] 第 2 条：图纸评测集表述 → "固定 100 条图纸评测集 Hit@1=xx%、P95=xx"（以 P0 新报告为准）
- [ ] 第 4 条：主集表述 → "250 条分层评测 Hit@1/3/5=xx%、MRR=xx、P95=xx"（以 P1 新报告为准）
- [ ] 第 4 条：RAGAS 表述 → "RAGAS 60 条分组评测 忠诚度/精准度/召回率"
- **验收**：简历中每个数字均有 `scripts/eval/report/` 对应报告支撑

## 五、不建议（保留后续考虑空间）

以下项当前优先级较低，暂不纳入本次执行，但后续可结合精力与目标再评估：

| 项 | 当前判断 | 后续可考虑的情形 |
|---|---|---|
| 口语集 19 → 50 | 简历未提口语条数 | 若要在面试中量化查询重写的多轮价值 |
| 业务场景 4 → 15-20 | 简历写的是四类分层，改场景 = 重做评测叙事 | 若要做更细的行业场景纵深 |
| 意图节点扩到 30 | 简历未提意图节点数 | 若想展示意图树体系规模 |
| 工业文档体系化 | 简历未提文档数 | 若想让知识库从 demo 数据升级为真实文档体系 |
| 以图搜图 / 多模态问答 | 属于新功能规划，与本清单（数据保真）无关 | 若产品侧需要"拍图提问"能力 |

## 六、风险与注意

- 图纸收集注意版权/授权（现有数据 `license` 字段缺省留空，不固化虚假授权信息）。
- RAGAS 打分依赖 `SILICONFLOW_API_KEY`（DeepSeek-V3.2）。
- 扩样本后指标必然变化：P1 重跑后 79%/81%/81%、MRR=0.7994 将不再适用，必须用新报告替换简历数字，不得沿用旧值。
- 若新指标明显下降（如 Hit@1 < 70%），需先定位原因（实体对齐/描述质量）再决定是否同步简历。

## 七、执行顺序建议

```
第 1-3 天：P0（图纸收集 40-50 张 + 描述 + 100 条评测）
第 3-4 天：P1（主集扩到 250 条 + 全链路重跑）
第 4-5 天：P2（RAGAS 60 条分组 + 报告）
第 5 天：P3（简历数字联动修正）
```
